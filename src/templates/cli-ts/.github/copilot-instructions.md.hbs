# {{projectName}}
# Stack: {{stack}}

This project uses Commander.js + chalk for TypeScript CLI.
Commands/ pattern for subcommand modules. Always exit non-zero on failure.
Vitest with mocked stdout/stdin. Handle SIGINT/SIGTERM.
Windows-safe paths: path.join() not string concat.
