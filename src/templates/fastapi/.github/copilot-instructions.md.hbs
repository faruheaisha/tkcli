# {{projectName}}
# Stack: {{stack}}

This project uses FastAPI with Pydantic v2 and async SQLAlchemy.
All route handlers async. Type hints everywhere — FastAPI relies on them.
Alembic for migrations, pytest + httpx for testing.
