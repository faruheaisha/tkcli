# {{projectName}}
# Stack: {{stack}}
# Description: {{description}}

This project uses Node.js with TypeScript (ESM). Build with tsc, dev with tsx.
Test with Vitest, co-located `.test.ts` files. Error handling: try-catch at async boundaries.
Always `import`/`export`, never `require()`. Pin dependencies, use `npm ci` in CI.
