# {{projectName}}

> {{description}}

## Windsurf AI Development Rules

### Stack: {{stack}}

- ESM + TypeScript strict mode
- Build: `npm run build` (tsc), dev: `npm run dev` (tsx)
- Test: `npm test` (vitest), co-located `.test.ts` files
- Error handling: try-catch at every async boundary
- Deps: typescript, tsx, vitest, @types/node
- No `require()` — ESM imports only
