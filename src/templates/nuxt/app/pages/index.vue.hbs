<template>
  <div>
    <h1>{{projectName}}</h1>
    <p>{{description}}</p>
    <div>
      <NuxtLink to="/about">About</NuxtLink>
    </div>
  </div>
</template>

<script setup lang="ts">
definePageMeta({
  title: "Home",
});

const { data: serverMessage } = await useAsyncData('hello', () =>
  $fetch('/api/hello')
);
</script>
