# {{projectName}}
# Stack: {{stack}}

This project uses Python with pyproject.toml build, src/ layout.
Type hints required everywhere — mypy in CI. pytest with coverage.
pathlib over os.path. Avoid mutable default args.
