# {{projectName}}
# Stack: {{stack}}

This project uses Rust with Cargo. thiserror for lib errors, anyhow for app errors.
Never .unwrap() in production code. Async with tokio.
Prefer &str over String in fn params. cargo test + cargo clippy must pass.
