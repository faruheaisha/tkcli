# {{projectName}}
# Stack: {{stack}}

This project uses Vue 3 Composition API (`<script setup lang="ts">`).
Pinia for state, Vue Router for routing, Vite for build.
Vitest + @vue/test-utils for testing. Avoid `reactive` on large objects.
